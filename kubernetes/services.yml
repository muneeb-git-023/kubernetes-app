apiVersion: v1
kind: Services
metadata:
    name: react-app-Services
spec:
  type: Nodeport
  selector:
    app: react-app
  ports:
    - port: 3000          
      targetPort: 3000    
      nodePort: 30080